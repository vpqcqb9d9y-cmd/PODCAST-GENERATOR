"""
Controller package for GUI domain logic.

This module groups controller classes that encapsulate window logic to keep
`app.py` lean. Controllers should avoid circular imports and expose clean
interfaces for the main window to delegate to.
"""

